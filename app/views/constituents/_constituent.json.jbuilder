json.extract! constituent, :id, :lookup_id, :suffix, :title, :name, :last_group, :email_id, :phone, :dob, :do_not_email, :duplicate, :created_at, :updated_at
json.url constituent_url(constituent, format: :json)
